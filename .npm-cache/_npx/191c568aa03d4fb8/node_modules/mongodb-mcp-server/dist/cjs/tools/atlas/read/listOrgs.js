"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ListOrganizationsTool = exports.ListOrganizationsArgs = void 0;
const zod_1 = require("zod");
const atlasTool_js_1 = require("../atlasTool.js");
const tool_js_1 = require("../../tool.js");
exports.ListOrganizationsArgs = {
    limit: zod_1.z.number().int().min(1).max(500).default(10).describe("Max number of organizations to return per page."),
    pageNum: zod_1.z.number().int().min(1).default(1).describe("Page number of organizations to return."),
};
const ListOrganizationsOutputSchema = {
    organizations: zod_1.z.array(zod_1.z.object({
        name: zod_1.z.string().optional(),
        id: zod_1.z.string().optional(),
    })),
    totalCount: zod_1.z.number(),
};
class ListOrganizationsTool extends atlasTool_js_1.AtlasToolBase {
    constructor() {
        super(...arguments);
        this.description = "List MongoDB Atlas organizations";
        this.argsShape = {
            ...exports.ListOrganizationsArgs,
        };
        this.outputSchema = ListOrganizationsOutputSchema;
    }
    async execute({ limit, pageNum }, context) {
        const data = await this.apiClient.listOrgs({
            params: {
                query: {
                    itemsPerPage: limit,
                    pageNum,
                },
            },
        }, context);
        if (!data?.results?.length) {
            return {
                content: [{ type: "text", text: "No organizations found in your MongoDB Atlas account." }],
                structuredContent: {
                    organizations: [],
                    totalCount: 0,
                },
            };
        }
        const orgs = data.results.map((org) => ({
            name: org.name,
            id: org.id,
        }));
        return {
            content: (0, tool_js_1.formatUntrustedData)(`Found ${orgs.length} organizations in your MongoDB Atlas account.`, JSON.stringify(orgs)),
            structuredContent: {
                organizations: orgs,
                totalCount: orgs.length,
            },
        };
    }
}
exports.ListOrganizationsTool = ListOrganizationsTool;
ListOrganizationsTool.toolName = "atlas-list-orgs";
ListOrganizationsTool.operationType = "read";
//# sourceMappingURL=listOrgs.js.map