import { z } from "zod";
import { MongoDBToolBase } from "../mongodbTool.js";
import type { ToolArgs, OperationType, ToolResult } from "../../tool.js";
import type { CallToolResult } from "@modelcontextprotocol/sdk/types.js";
import type { ConnectionMetadata } from "../../../telemetry/types.js";
export declare class ConnectTool extends MongoDBToolBase {
    static toolName: string;
    description: string;
    argsShape: {
        connectionString: z.ZodString;
        connectionName: z.ZodOptional<z.ZodString>;
    };
    static operationType: OperationType;
    outputSchema: {
        connectionId: z.ZodString;
    };
    protected execute({ connectionString, connectionName, }: ToolArgs<typeof this.argsShape>): Promise<ToolResult<typeof this.outputSchema>>;
    protected resolveTelemetryMetadata(args: ToolArgs<typeof this.argsShape>, { result }: {
        result: CallToolResult;
    }): Promise<ConnectionMetadata>;
}
//# sourceMappingURL=connect.d.ts.map