import type { Client } from "@mongodb-js/atlas-local";
/** Keep well under the MCP client's default ~60s request timeout. */
export declare const DEFAULT_MAX_ATTEMPTS = 60;
export declare const DEFAULT_INTERVAL_MS = 500;
export declare class AtlasLocalDeploymentNotReadyError extends Error {
    readonly deploymentName: string;
    constructor(deploymentName: string);
}
/**
 * atlas-local-create-deployment can return before Docker publishes port bindings.
 * Retry briefly so connect usually works without exceeding MCP request timeouts.
 */
export declare function waitForConnectionString(client: Client, deploymentName: string, { maxAttempts, intervalMs, }?: {
    maxAttempts?: number;
    intervalMs?: number;
}): Promise<string>;
//# sourceMappingURL=connectionString.d.ts.map