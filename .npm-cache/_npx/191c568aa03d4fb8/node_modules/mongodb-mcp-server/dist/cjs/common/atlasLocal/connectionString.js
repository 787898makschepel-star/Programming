"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.AtlasLocalDeploymentNotReadyError = exports.DEFAULT_INTERVAL_MS = exports.DEFAULT_MAX_ATTEMPTS = void 0;
exports.waitForConnectionString = waitForConnectionString;
const managedTimeout_js_1 = require("../managedTimeout.js");
/** Keep well under the MCP client's default ~60s request timeout. */
exports.DEFAULT_MAX_ATTEMPTS = 60;
exports.DEFAULT_INTERVAL_MS = 500;
class AtlasLocalDeploymentNotReadyError extends Error {
    constructor(deploymentName) {
        super(`Atlas Local deployment "${deploymentName}" is still starting up`);
        this.deploymentName = deploymentName;
        this.name = "AtlasLocalDeploymentNotReadyError";
    }
}
exports.AtlasLocalDeploymentNotReadyError = AtlasLocalDeploymentNotReadyError;
function isMissingPortBindingError(error) {
    const message = error instanceof Error ? error.message : String(error);
    return message.includes("Missing port binding information");
}
/**
 * atlas-local-create-deployment can return before Docker publishes port bindings.
 * Retry briefly so connect usually works without exceeding MCP request timeouts.
 */
async function waitForConnectionString(client, deploymentName, { maxAttempts = exports.DEFAULT_MAX_ATTEMPTS, intervalMs = exports.DEFAULT_INTERVAL_MS, } = {}) {
    for (let attempt = 0; attempt < maxAttempts; attempt++) {
        try {
            return await client.getConnectionString(deploymentName);
        }
        catch (error) {
            if (!isMissingPortBindingError(error)) {
                throw error;
            }
            if (attempt < maxAttempts - 1) {
                await (0, managedTimeout_js_1.sleep)(intervalMs);
            }
        }
    }
    throw new AtlasLocalDeploymentNotReadyError(deploymentName);
}
//# sourceMappingURL=connectionString.js.map