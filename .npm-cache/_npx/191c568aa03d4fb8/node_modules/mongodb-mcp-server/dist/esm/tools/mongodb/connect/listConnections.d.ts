import { z } from "zod";
import { MongoDBToolBase } from "../mongodbTool.js";
import type { OperationType, ToolResult } from "../../tool.js";
export declare class ListConnectionsTool extends MongoDBToolBase {
    static toolName: string;
    description: string;
    argsShape: {};
    static operationType: OperationType;
    outputSchema: {
        connections: z.ZodArray<z.ZodObject<{
            connectionId: z.ZodString;
            name: z.ZodString;
            source: z.ZodEnum<{
                preconfigured: "preconfigured";
                explicit: "explicit";
            }>;
            state: z.ZodOptional<z.ZodEnum<{
                errored: "errored";
                connected: "connected";
                connecting: "connecting";
                disconnected: "disconnected";
            }>>;
            description: z.ZodString;
            lastError: z.ZodOptional<z.ZodString>;
            createdAt: z.ZodString;
            lastUsedAt: z.ZodString;
        }, z.core.$strip>>;
    };
    protected execute(): Promise<ToolResult<typeof this.outputSchema>>;
    private noConnectionsText;
}
//# sourceMappingURL=listConnections.d.ts.map