import { z } from "zod";
import { MongoDBToolBase } from "../mongodbTool.js";
import type { ToolArgs, OperationType, ToolResult } from "../../tool.js";
export declare class DisconnectTool extends MongoDBToolBase {
    static toolName: string;
    description: string;
    argsShape: {
        connectionId: z.ZodString;
    };
    static operationType: OperationType;
    outputSchema: {
        outcome: z.ZodEnum<{
            disconnected: "disconnected";
            removed: "removed";
        }>;
    };
    protected execute({ connectionId, }: ToolArgs<typeof this.argsShape>): Promise<ToolResult<typeof this.outputSchema>>;
}
//# sourceMappingURL=disconnect.d.ts.map