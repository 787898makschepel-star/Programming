import { z } from "zod";
import { type ToolArgs, type OperationType, type ToolExecutionContext, type ToolResult } from "../../tool.js";
import { AtlasToolBase } from "../atlasTool.js";
export declare class CreateFreeClusterTool extends AtlasToolBase {
    static toolName: string;
    description: string;
    static operationType: OperationType;
    argsShape: {
        projectId: z.ZodString;
        name: z.ZodString;
        region: z.ZodDefault<z.ZodString>;
    };
    outputSchema: {
        created: z.ZodBoolean;
    };
    protected execute({ projectId, name, region }: ToolArgs<typeof this.argsShape>, context: ToolExecutionContext): Promise<ToolResult<typeof this.outputSchema>>;
}
//# sourceMappingURL=createFreeCluster.d.ts.map